# F3 center-trace masked periodic-refresh artifact producers

This experiment preserves the center-trace scientific fields while adding the
fixed periodic student-embedding/HMM-center refresh schedule. Its smoke and
full roots are isolated, and the complete validator requires the final
generation, selected checkpoint, embedding metadata, and immutable target
identity.

Run stages `01` through `04` in order with
`train_strat_hmm_pretext.py`, `extract_embeddings.py`, and
`validate_f3_center_trace_masked_periodic_refresh.py`. See
[the scientific contract](../../../../docs/f3_center_trace_masked_periodic_hmm_refresh_plan.md)
for refresh semantics.

Complete outputs remain under `artifacts/seis_ssl_cluster/`.
