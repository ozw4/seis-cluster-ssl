# F3 center-trace periodic-refresh producers

The scientific question and interpretation limits are recorded in
[`docs/f3_center_trace_masked_periodic_hmm_refresh_plan.md`](../../../../docs/f3_center_trace_masked_periodic_hmm_refresh_plan.md).
This directory owns only the executable conditions.

Run the numbered YAML stages in order: isolated smoke, full pretraining,
embedding extraction, and complete validation. Exact refresh settings,
checkpoint selection, inputs, and outputs belong to the YAML, implementation,
and validator.
