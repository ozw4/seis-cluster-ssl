[pytest]
testpaths = tests
markers =
	unit: pure unit tests
	integration: multiple modules, subprocess, proc entrypoint, or filesystem-heavy tests
	smoke: end-to-end or stage-level smoke tests intended for quick contract checks
	slow: locally heavier tests
	requires_segy: tests requiring real external SEGY data
	requires_cuda: tests requiring CUDA
